require("dotenv").config();
const StripeService=require("../services/stripe.service");
(async()=>{try{const id=String(process.argv[2]||"").trim();if(!id.startsWith("pi_"))throw new Error("Usage : node scripts/cancel-stripe-test-payment.js pi_...");const cfg=StripeService.getConfig();if(cfg.environment!=="test")throw new Error("Ce script est strictement réservé à Stripe TEST.");const current=await StripeService.retrievePaymentIntent(id);if(current.status==="succeeded")throw new Error("Un PaymentIntent réussi doit être remboursé, pas annulé.");if(current.status==="canceled"){console.log("PaymentIntent déjà annulé.");process.exit(0);}const result=await StripeService.getClient().paymentIntents.cancel(id);StripeService.assertStripeObjectMode(result.livemode);console.log("✅ PaymentIntent TEST annulé :",{id:result.id,status:result.status});process.exit(0);}catch(e){console.error("❌ Annulation Stripe TEST :",e.message);process.exit(1);}})();
