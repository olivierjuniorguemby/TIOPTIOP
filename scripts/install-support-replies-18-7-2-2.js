require('dotenv').config();
const db=require('../config/database');
(async()=>{try{const cols=await db.query("SHOW COLUMNS FROM support_messages LIKE 'reply_to_message_id'");if(!cols.length){await db.query('ALTER TABLE support_messages ADD COLUMN reply_to_message_id BIGINT NULL AFTER message');console.log('OK - reply_to_message_id ajoutée');}else console.log('OK - reply_to_message_id existe déjà');const fks=await db.query("SELECT CONSTRAINT_NAME FROM information_schema.KEY_COLUMN_USAGE WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='support_messages' AND COLUMN_NAME='reply_to_message_id' AND REFERENCED_TABLE_NAME='support_messages'");if(!fks.length){await db.query('ALTER TABLE support_messages ADD CONSTRAINT fk_support_messages_reply FOREIGN KEY (reply_to_message_id) REFERENCES support_messages(id) ON DELETE SET NULL');console.log('OK - FK réponse ajoutée');}else console.log('OK - FK réponse existe déjà');console.log('OK - Support 18.7.2.2 prêt');process.exit(0);}catch(e){console.error('ERREUR 18.7.2.2:',e);process.exit(1);}})();
